version: '3'

services:
  db:
    image: postgres:14-alpine
    restart: on-failure
    ports:
      - 8083:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=db
